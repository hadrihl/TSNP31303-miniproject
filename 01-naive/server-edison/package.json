{
  "name": "server-edison",
  "version": "0.0.1",
  "description": "An Intel Edison sensor node code for sensor (uv, temperature and humidity) monitoring using Socket.IO.",
  "main": "server-edison.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "hadrihilmi@gmail.com",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.2",
    "socket.io": "^2.0.4"
  },
  "devDependencies": {}
}
